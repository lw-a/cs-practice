// Node
const Node = (value) => {

  return { value, nextNode: null };
};

const test = Node(19);
console.log(test.value)
console.log(test.nextNode)

// Linked List
const LinkedList = () => {

  return { };
};
